{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "defaultBase": "origin/main",
  "targetDefaults": {
    "test": {
      "cache": true,
      "inputs": ["default", "^default"]
    },
    "typecheck": {
      "cache": true,
      "inputs": ["default", "^default"]
    },
    "build": {
      "cache": true,
      "inputs": ["default", "^default"]
    }
  }
}
